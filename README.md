Number Guessing Game 🎯
  A simple interactive number guessing game built with JavaScript. The player tries to guess a randomly generated number within a set range, receiving feedback on each guess.

Features
  Randomly generates a number between 1 and 100
  Tracks the number of attempts
  Provides feedback:
    “Too low” if the guess is smaller than the answer
    “Too high” if the guess is larger than the answer
    “Correct” when the number is guessed
  Runs in the browser using window.prompt and window.alert

How It Works
  The game generates a random number between a defined minimum and maximum.
  The player is prompted to enter a guess.
  The game checks if the input is valid:
    Must be a number
    Must be within the specified range
  Provides feedback after each guess and counts the attempts.
  The game ends when the correct number is guessed.

